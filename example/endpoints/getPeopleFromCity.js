const {ArrayType, Enum0} = require("../types.js");

const {City, Person} = require("../models.js");

/**
 * res_output
 * @param person: ArrayType(Person)
 */

/**
 * res_input
 * = City
 */
module.exports = async function process_getPeopleFromCity(res_input, res_output) {
	// TODO: implement
	return res_output;
}